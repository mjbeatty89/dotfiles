---
# Ansible Inventory - Matthew's Machines
all:
  children:
    macos:
      hosts:
        mmm4:
          ansible_host: localhost
          ansible_connection: local
          external_home: /Volumes/mm2ssd/mjb
          old_home: /Users/mjb
          
    linux:
      hosts:
        # linux-desktop:
        #   ansible_host: linux-desktop.local
        
    servers:
      hosts:
        # unraid:
        #   ansible_host: unraid.local
        # proxmox:
        #   ansible_host: proxmox.local
        
    raspberry_pi:
      hosts:
        # rpi5:
        #   ansible_host: rpi5.local
